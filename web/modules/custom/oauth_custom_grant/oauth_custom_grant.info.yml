name: OAuth Custom Grant
description: 'Provides custom oauth grant types to support Gmail, Username/Password sign-in and Firebase OTP'
package: Hucha Custom

type: module
core_version_requirement: ^10 || ^11
version: '10.x-1.0'
dependencies:
  - rest:rest
  - drupal:user
  - simple_oauth:simple_oauth
